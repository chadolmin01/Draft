"use client"

/**
 * Toaster Hook for easy toast usage throughout the app
 * Import and use this in components instead of toast.tsx directly
 */

export { useToast, Toaster, ToastProvider } from "./toast"
export type { Toast, ToastVariant } from "./toast"
